# Committed. The real .env is never committed - see C3.2.
# Every variable the project reads must appear here, with a safe placeholder
# or a comment saying where the real value comes from.

# APP_ENV=development
# DATABASE_URL=postgres://app:app@localhost:5432/app
# SECRET_KEY=            # generate: openssl rand -hex 32
